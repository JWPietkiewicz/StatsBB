<Application x:Class="StatsBB.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:StatsBB"
             xmlns:ui="http://schemas.modernwpf.com/2019"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ui:ThemeResources />
                <ui:XamlControlsResources />
            </ResourceDictionary.MergedDictionaries>
            
            <!-- Global FluentUI System Colors -->
            <SolidColorBrush x:Key="SystemBackgroundColor" Color="#1E1E1E"/>
            <SolidColorBrush x:Key="SystemSurfaceColor" Color="#2D2D30"/>
            <SolidColorBrush x:Key="SystemControlBackgroundColor" Color="#3F3F46"/>
            <SolidColorBrush x:Key="SystemControlHoverColor" Color="#484848"/>
            <SolidColorBrush x:Key="SystemControlPressedColor" Color="#525252"/>
            <SolidColorBrush x:Key="SystemTextColor" Color="#FFFFFF"/>
            <SolidColorBrush x:Key="SystemTextSecondaryColor" Color="#C8C8C8"/>
            <SolidColorBrush x:Key="SystemBorderColor" Color="#5A5A5A"/>
            <SolidColorBrush x:Key="DisabledColor" Color="#5A5A5A"/>
            
            <!-- Semantic Colors -->
            <SolidColorBrush x:Key="SuccessColor" Color="#00B373"/>
            <SolidColorBrush x:Key="ErrorColor" Color="#D83B01"/>
            <SolidColorBrush x:Key="WarningColor" Color="#F7B801"/>
            
            <!-- FluentUI DataGrid Styles -->
            <Style TargetType="DataGrid">
                <Setter Property="Background" Value="{StaticResource SystemSurfaceColor}"/>
                <Setter Property="Foreground" Value="{StaticResource SystemTextColor}"/>
                <Setter Property="BorderBrush" Value="{StaticResource SystemBorderColor}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="GridLinesVisibility" Value="Horizontal"/>
                <Setter Property="HorizontalGridLinesBrush" Value="{StaticResource SystemBorderColor}"/>
                <Setter Property="VerticalGridLinesBrush" Value="{StaticResource SystemBorderColor}"/>
                <Setter Property="RowBackground" Value="Transparent"/>
                <Setter Property="AlternatingRowBackground" Value="{StaticResource SystemControlBackgroundColor}"/>
            </Style>
            
            <Style TargetType="DataGridColumnHeader">
                <Setter Property="Background" Value="{StaticResource SystemControlBackgroundColor}"/>
                <Setter Property="Foreground" Value="{StaticResource SystemTextColor}"/>
                <Setter Property="BorderBrush" Value="{StaticResource SystemBorderColor}"/>
                <Setter Property="BorderThickness" Value="0,0,1,1"/>
                <Setter Property="Padding" Value="8,4"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
            </Style>
            
            <Style TargetType="DataGridCell">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Foreground" Value="{StaticResource SystemTextColor}"/>
                <Setter Property="BorderBrush" Value="Transparent"/>
                <Setter Property="Padding" Value="8,4"/>
                <Style.Triggers>
                    <Trigger Property="IsSelected" Value="True">
                        <Setter Property="Background" Value="{StaticResource SystemControlHoverColor}"/>
                        <Setter Property="Foreground" Value="{StaticResource SystemTextColor}"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
            
            <!-- FluentUI ComboBox Styles -->
            <Style TargetType="ComboBox">
                <Setter Property="Background" Value="{StaticResource SystemControlBackgroundColor}"/>
                <Setter Property="Foreground" Value="{StaticResource SystemTextColor}"/>
                <Setter Property="BorderBrush" Value="{StaticResource SystemBorderColor}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Padding" Value="8,4"/>
                <Setter Property="FontSize" Value="14"/>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="{StaticResource SystemControlHoverColor}"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
            
            <!-- FluentUI Border Styles -->
            <Style x:Key="FluentPanelBorder" TargetType="Border">
                <Setter Property="Background" Value="{StaticResource SystemSurfaceColor}"/>
                <Setter Property="BorderBrush" Value="{StaticResource SystemBorderColor}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="CornerRadius" Value="4"/>
            </Style>
            
            <Style x:Key="FluentCourtBorder" TargetType="Border">
                <Setter Property="Background" Value="{StaticResource SystemControlBackgroundColor}"/>
                <Setter Property="BorderBrush" Value="{StaticResource SystemBorderColor}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="CornerRadius" Value="4"/>
            </Style>
            
            <Style x:Key="FluentBenchBorder" TargetType="Border">
                <Setter Property="Background" Value="{StaticResource SystemSurfaceColor}"/>
                <Setter Property="BorderBrush" Value="{StaticResource SystemBorderColor}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="CornerRadius" Value="4"/>
            </Style>
            
            <!-- Global Button Styles -->
            <Style x:Key="BaseActionButtonStyle" TargetType="Button">
                <Setter Property="Background" Value="{StaticResource SystemControlBackgroundColor}"/>
                <Setter Property="Foreground" Value="{StaticResource SystemTextColor}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="BorderBrush" Value="{StaticResource SystemBorderColor}"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="Margin" Value="4" />
                <Setter Property="Padding" Value="12,8"/>
                <Setter Property="MinHeight" Value="32"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="4"
                        Padding="{TemplateBinding Padding}">
                                <ContentPresenter
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            RecognizesAccessKey="True"/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>

                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="{StaticResource SystemControlHoverColor}"/>
                        <Setter Property="Cursor" Value="Hand"/>
                    </Trigger>
                    <Trigger Property="IsPressed" Value="True">
                        <Setter Property="Background" Value="{StaticResource SystemControlPressedColor}"/>
                    </Trigger>
                    <Trigger Property="IsEnabled" Value="False">
                        <Setter Property="Background" Value="{StaticResource DisabledColor}"/>
                        <Setter Property="Foreground" Value="{StaticResource SystemTextSecondaryColor}"/>
                        <Setter Property="Opacity" Value="0.6"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
            
            <!-- FluentUI CheckBox Style -->
            <Style TargetType="CheckBox">
                <Setter Property="Foreground" Value="{StaticResource SystemTextColor}"/>
                <Setter Property="FontWeight" Value="Normal"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="Margin" Value="4"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="CheckBox">
                            <StackPanel Orientation="Horizontal">
                                <Border x:Name="CheckBorder" Width="20" Height="20" 
                                        Background="{StaticResource SystemControlBackgroundColor}"
                                        BorderBrush="{StaticResource SystemBorderColor}"
                                        BorderThickness="2" CornerRadius="3" Margin="0,0,8,0">
                                    <ui:FontIcon x:Name="CheckIcon" Glyph="&#xE73E;" FontSize="12" 
                                                Foreground="{StaticResource SuccessColor}" 
                                                Visibility="Collapsed" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ContentPresenter VerticalAlignment="Center" RecognizesAccessKey="True"/>
                            </StackPanel>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="CheckIcon" Property="Visibility" Value="Visible"/>
                                    <Setter TargetName="CheckBorder" Property="Background" Value="{StaticResource SystemControlHoverColor}"/>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="CheckBorder" Property="BorderBrush" Value="{StaticResource SystemTextColor}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            
            <!-- FluentUI RadioButton Style -->
            <Style TargetType="RadioButton">
                <Setter Property="Foreground" Value="{StaticResource SystemTextColor}"/>
                <Setter Property="FontWeight" Value="Normal"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="Margin" Value="4"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="RadioButton">
                            <StackPanel Orientation="Horizontal">
                                <Border x:Name="RadioBorder" Width="20" Height="20" 
                                        Background="{StaticResource SystemControlBackgroundColor}"
                                        BorderBrush="{StaticResource SystemBorderColor}"
                                        BorderThickness="2" CornerRadius="10" Margin="0,0,8,0">
                                    <Ellipse x:Name="RadioDot" Width="10" Height="10" 
                                            Fill="{StaticResource SuccessColor}" 
                                            Visibility="Collapsed" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ContentPresenter VerticalAlignment="Center" RecognizesAccessKey="True"/>
                            </StackPanel>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="RadioDot" Property="Visibility" Value="Visible"/>
                                    <Setter TargetName="RadioBorder" Property="Background" Value="{StaticResource SystemControlHoverColor}"/>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="RadioBorder" Property="BorderBrush" Value="{StaticResource SystemTextColor}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            
            <!-- FluentUI TextBox Style -->
            <Style x:Key="DefaultTextBoxStyle" TargetType="TextBox">
                <Setter Property="Background" Value="{StaticResource SystemControlBackgroundColor}"/>
                <Setter Property="Foreground" Value="{StaticResource SystemTextColor}"/>
                <Setter Property="BorderBrush" Value="{StaticResource SystemBorderColor}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Padding" Value="8,4"/>
                <Setter Property="FontSize" Value="14"/>
                <Style.Triggers>
                    <Trigger Property="IsFocused" Value="True">
                        <Setter Property="BorderBrush" Value="{StaticResource SuccessColor}"/>
                        <Setter Property="BorderThickness" Value="2"/>
                    </Trigger>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="{StaticResource SystemControlHoverColor}"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
