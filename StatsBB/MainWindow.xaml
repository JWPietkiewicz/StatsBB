<Window x:Class="StatsBB.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:StatsBB.UserControls" 
        xmlns:viewmodel="clr-namespace:StatsBB.ViewModel" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:converters="clr-namespace:StatsBB.Converters"
        xmlns:po="http://schemas.microsoft.com/winfx/2006/xaml/presentation/options"
        Title="Basketball Stats Map" Height="1080" Width="1920"
        PreviewKeyDown="Window_PreviewKeyDown">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <converters:ColorAvailabilityConverter x:Key="ColorAvailabilityConverter"/>
        <converters:ColorSelectedConverter x:Key="ColorSelectedConverter"/>


        <SolidColorBrush x:Key="PrimaryAColor" Color="Orange" po:Freeze="False"/>
        <SolidColorBrush x:Key="SecondaryAColor" Color="White" po:Freeze="False"/>
        <SolidColorBrush x:Key="PrimaryBColor" Color="Green" po:Freeze="False"/>
        <SolidColorBrush x:Key="SecondaryBColor" Color="White" po:Freeze="False"/>
        <SolidColorBrush x:Key="BenchColor" Color="LightGray"/>
        <SolidColorBrush x:Key="DisabledColor" Color="LightGray"/>
        <SolidColorBrush x:Key="HoverGrey" Color="#303030"/>
        <SolidColorBrush x:Key="SelectedMadeColor" Color="Green"/>
        <SolidColorBrush x:Key="SelectedMissedColor" Color="Red"/>
        <SolidColorBrush x:Key="DarkGrey" Color="DarkGray"/>
        <SolidColorBrush x:Key="Grey" Color="Gray"/>

        <Style x:Key="BasePlayerButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="64" />
            <Setter Property="Height" Value="64" />
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="24"/>
        </Style>
        <Style x:Key="BaseActionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Gray"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="Margin" Value="5" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border
                    Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="2"
                    Padding="{TemplateBinding Padding}">
                            <ContentPresenter
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        RecognizesAccessKey="True"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>

            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource HoverGrey}"/>
                    <Setter Property="Cursor" Value="Hand"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="ActionSelectableButtonStyle" TargetType="Button" BasedOn="{StaticResource BaseActionButtonStyle}">
            <Setter Property="Background" Value="{StaticResource DarkGrey}"/>
        </Style>
        <!-- Action: Cannot be selected -->
        <Style x:Key="ActionDisabledButtonStyle" TargetType="Button" BasedOn="{StaticResource BaseActionButtonStyle}">
            <Setter Property="Background" Value="{StaticResource DisabledColor}"/>
        </Style>

        <!-- Action: Selected Made -->
        <Style x:Key="ActionSelectedMadeStyle" TargetType="Button" BasedOn="{StaticResource BaseActionButtonStyle}">
            <Setter Property="Background" Value="{StaticResource SelectedMadeColor}"/>
        </Style>

        <!-- Action: Selected Missed, Foul, Turnover -->
        <Style x:Key="ActionSelectedFailedStyle" TargetType="Button" BasedOn="{StaticResource BaseActionButtonStyle}">
            <Setter Property="Background" Value="{StaticResource SelectedMissedColor}"/>
        </Style>
        <!-- Court Player: Not Selectable -->
        <Style x:Key="CourtPlayerDisabledStyle" TargetType="Button" BasedOn="{StaticResource BasePlayerButtonStyle}" po:Freeze="False">
            <!--<Setter Property="Background" Value="{Binding TeamColorDimmed}" />-->
            <Setter Property="Background" Value="Gray"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>

        <!-- Court Player: Selectable (Team-colored) -->
        <Style x:Key="CourtPlayerSelectableStyle" TargetType="Button" BasedOn="{StaticResource BasePlayerButtonStyle}" po:Freeze="False">
            <Setter Property="Background" Value="{Binding TeamColor}" />
            <!-- via VM -->
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="0.8"/>
                    <!-- hover effect -->
                </Trigger>
            </Style.Triggers>
        </Style>
        <!-- Bench Player: Not Selectable -->
        <Style x:Key="BenchPlayerDisabledStyle" TargetType="Button" BasedOn="{StaticResource BasePlayerButtonStyle}" po:Freeze="False">
            <Setter Property="Background" Value="{StaticResource Grey}"/>
            <Setter Property="Foreground" Value="DarkGray"/>
        </Style>

        <!-- Bench Player: Selectable -->
        <Style x:Key="BenchPlayerSelectableStyle" TargetType="Button" BasedOn="{StaticResource BasePlayerButtonStyle}" po:Freeze="False">
            <Setter Property="Background" Value="{StaticResource Grey}"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="BorderBrush" Value="{Binding TeamColor}" />
            <!-- via VM -->
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource HoverGrey}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="BaseButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="24"/>
        </Style>

        <Style x:Key="BenchPlayerButtonStyle" TargetType="Button" BasedOn="{StaticResource BasePlayerButtonStyle}"  po:Freeze="False">
            <Setter Property="Background" Value="{StaticResource BenchColor}"/>
        </Style>
        <Style x:Key="CourtAPlayerButtonStyle" TargetType="Button" BasedOn="{StaticResource BasePlayerButtonStyle}" po:Freeze="False">
            <Setter Property="Background" Value="{DynamicResource PrimaryAColor}"/>
            <Setter Property="Foreground" Value="{DynamicResource SecondaryAColor}"/>
        </Style>
        <Style x:Key="BenchAPlayerButtonStyle" TargetType="Button" BasedOn="{StaticResource BasePlayerButtonStyle}" po:Freeze="False">
            <Setter Property="Background" Value="{DynamicResource SecondaryAColor}"/>
            <Setter Property="Foreground" Value="{DynamicResource PrimaryAColor}" />
            <Setter Property="BorderBrush" Value="{DynamicResource PrimaryAColor}" />
            <Setter Property="BorderThickness" Value="1" />
        </Style>
        <Style x:Key="BenchBPlayerButtonStyle" TargetType="Button" BasedOn="{StaticResource BasePlayerButtonStyle}" po:Freeze="False">
            <Setter Property="Background" Value="{DynamicResource SecondaryBColor}"/>
            <Setter Property="Foreground" Value="{DynamicResource PrimaryBColor}" />
            <Setter Property="BorderBrush" Value="{DynamicResource PrimaryBColor}" />
            <Setter Property="BorderThickness" Value="1" />
        </Style>
        <Style x:Key="CourtBPlayerButtonStyle" TargetType="Button" BasedOn="{StaticResource BasePlayerButtonStyle}" po:Freeze="False">
            <Setter Property="Background" Value="{DynamicResource PrimaryBColor}" />
            <Setter Property="Foreground" Value="{DynamicResource SecondaryBColor}"/>
        </Style>
        <Style x:Key="BenchButton" TargetType="Button" BasedOn="{StaticResource BaseButtonStyle}" po:Freeze="False">
        </Style>

        <Style x:Key="BottomPanelButton" BasedOn="{StaticResource BaseActionButtonStyle}" TargetType="Button">
            <Setter Property="Padding" Value="15"/> 
        </Style>
        <Style x:Key="Made" TargetType="Button" BasedOn="{StaticResource BottomPanelButton}"/>
        <Style x:Key="Missed" TargetType="Button" BasedOn="{StaticResource BottomPanelButton}"/>
        <Style x:Key="Sub" TargetType="Button" BasedOn="{StaticResource BottomPanelButton}"/>
        <Style x:Key="Foul" TargetType="Button" BasedOn="{StaticResource BottomPanelButton}"/>
        <Style x:Key="Turnover" TargetType="Button" BasedOn="{StaticResource BottomPanelButton}"/>
        <Style x:Key="JumpBall" TargetType="Button" BasedOn="{StaticResource BottomPanelButton}"/>
        <Style x:Key="TimeOut" TargetType="Button" BasedOn="{StaticResource BottomPanelButton}"/>
        <Style x:Key="FreeThrows" TargetType="Button" BasedOn="{StaticResource BottomPanelButton}"/>
        <Style x:Key="Assist" TargetType="Button" BasedOn="{StaticResource BottomPanelButton}"/>
        <Style x:Key="Rebound" TargetType="Button" BasedOn="{StaticResource BottomPanelButton}"/>
        <Style x:Key="Steal" TargetType="Button" BasedOn="{StaticResource BottomPanelButton}"/>
        <!-- TEAM A TIMEOUT BUTTON -->
        <Style x:Key="TeamATimeoutStyle" TargetType="Button" BasedOn="{StaticResource BaseActionButtonStyle}" po:Freeze="False">
            <Setter Property="Background" Value="{DynamicResource PrimaryAColor}" />
            <Setter Property="Foreground" Value="{DynamicResource SecondaryAColor}" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Width" Value="120" />
            <Setter Property="Height" Value="80" />
        </Style>

        <!-- TEAM B TIMEOUT BUTTON -->
        <Style x:Key="TeamBTimeoutStyle" TargetType="Button" BasedOn="{StaticResource BaseActionButtonStyle}" po:Freeze="False">
            <Setter Property="Background" Value="{DynamicResource PrimaryBColor}" />
            <Setter Property="Foreground" Value="{DynamicResource SecondaryBColor}" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Width" Value="120" />
            <Setter Property="Height" Value="80" />
        </Style>

        <Style x:Key="SubButtonStyle" TargetType="Button" BasedOn="{StaticResource BasePlayerButtonStyle}">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="Black"/>
        </Style>
        <!-- TEAM A REBOUND BUTTON -->
        <Style x:Key="TeamAReboundStyle" TargetType="Button" BasedOn="{StaticResource BaseActionButtonStyle}" po:Freeze="False">
            <Setter Property="Background" Value="{DynamicResource PrimaryAColor}" />
            <Setter Property="Foreground" Value="{DynamicResource SecondaryAColor}" />
            <Setter Property="FontWeight" Value="Bold" />
        </Style>


        <!-- TEAM B REBOUND BUTTON -->
        <Style x:Key="TeamBReboundStyle" TargetType="Button" BasedOn="{StaticResource BaseActionButtonStyle}" po:Freeze="False">
            <Setter Property="Background" Value="{DynamicResource PrimaryBColor}" />
            <Setter Property="Foreground" Value="{DynamicResource SecondaryBColor}" />
            <Setter Property="FontWeight" Value="Bold" />
        </Style>

        <!-- TEAM A TECHNICAL FOUL BUTTON STYLE-->
        <Style x:Key="CoachTechnicalTeamAStyle" TargetType="Button" BasedOn="{StaticResource BaseActionButtonStyle}" po:Freeze="False">
            <Setter Property="Background" Value="{DynamicResource PrimaryAColor}"  />
            <Setter Property="Foreground" Value="{DynamicResource SecondaryAColor}" />
            <Setter Property="BorderBrush" Value="{DynamicResource SecondaryAColor}" />
            <Setter Property="FontSize" Value="22" />
            <Setter Property="BorderThickness" Value="3" />
            <Setter Property="FontWeight" Value="Bold" />
        </Style>
        <Style x:Key="BenchTechnicalTeamAStyle" TargetType="Button" BasedOn="{StaticResource BaseActionButtonStyle}" po:Freeze="False">
            <Setter Property="Background" Value="{DynamicResource SecondaryAColor}"  />
            <Setter Property="Foreground" Value="{DynamicResource PrimaryAColor}" />
            <Setter Property="BorderBrush" Value="{DynamicResource PrimaryAColor}" />
            <Setter Property="BorderThickness" Value="3" />
            <Setter Property="FontSize" Value="22" />
            <Setter Property="FontWeight" Value="Bold" />
        </Style>
        <!-- TEAM B TECHNICAL FOUL BUTTON STYLE-->
        <Style x:Key="CoachTechnicalTeamBStyle" TargetType="Button" BasedOn="{StaticResource BaseActionButtonStyle}" po:Freeze="False">
            <Setter Property="Background" Value="{DynamicResource PrimaryBColor}" />
            <Setter Property="Foreground" Value="{DynamicResource SecondaryBColor}"  />
            <Setter Property="BorderBrush" Value="{DynamicResource SecondaryBColor}"  />
            <Setter Property="FontSize" Value="22" />
            <Setter Property="BorderThickness" Value="3" />
            <Setter Property="FontWeight" Value="Bold" />
        </Style>
        <Style x:Key="BenchTechnicalTeamBStyle" TargetType="Button" BasedOn="{StaticResource BaseActionButtonStyle}" po:Freeze="False">
            <Setter Property="Background" Value="{DynamicResource SecondaryBColor}" />
            <Setter Property="Foreground" Value="{DynamicResource PrimaryBColor}"  />
            <Setter Property="BorderBrush" Value="{DynamicResource PrimaryBColor}" />
            <Setter Property="FontSize" Value="22" />
            <Setter Property="BorderThickness" Value="3" />
            <Setter Property="FontWeight" Value="Bold" />
        </Style>
        <!-- TEAM A TURNOVER BUTTON -->
        <Style x:Key="TeamATurnoverStyle" TargetType="Button" BasedOn="{StaticResource BaseActionButtonStyle}" po:Freeze="False">
            <Setter Property="Background" Value="{DynamicResource PrimaryAColor}"  />
            <Setter Property="Foreground" Value="{DynamicResource SecondaryAColor}" />
            <Setter Property="FontWeight" Value="Bold" />
        </Style>

        <!-- TEAM B TURNOVER BUTTON -->
        <Style x:Key="TeamBTurnoverStyle" TargetType="Button" BasedOn="{StaticResource BaseActionButtonStyle}" po:Freeze="False">
            <Setter Property="Background" Value="{DynamicResource PrimaryBColor}" />
            <Setter Property="Foreground" Value="{DynamicResource SecondaryBColor}"  />
            <Setter Property="FontWeight" Value="Bold" />
        </Style>

        <!-- BLOCK BUTTON -->
        <Style x:Key="BlockStyle" TargetType="Button" BasedOn="{StaticResource BaseActionButtonStyle}">
            <Setter Property="Background" Value="Gray" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Width" Value="60" />
            <Setter Property="Height" Value="40" />
        </Style>

        <!-- SHOT CLOCK VIOLATION BUTTON -->
        <Style x:Key="ShotClockStyle" TargetType="Button" BasedOn="{StaticResource BaseActionButtonStyle}">
            <Setter Property="Background" Value="Gray" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Width" Value="60" />
            <Setter Property="Height" Value="40" />
        </Style>

        <!-- FOUL TYPE BUTTON -->
        <Style x:Key="FoulTypeButtonStyle" TargetType="Button" BasedOn="{StaticResource BaseActionButtonStyle}">
            <Setter Property="FontSize" Value="18" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Height" Value="60" />
            <Setter Property="Width" Value="280" />
        </Style>
        <!-- FREE THROW COUNT BUTTON -->
        <Style x:Key="FreeThrowCountButtonStyle" TargetType="Button" BasedOn="{StaticResource FoulTypeButtonStyle}">
            <Setter Property="Width" Value="60" />
            <Setter Property="Height" Value="60" />
        </Style>
        <Style x:Key="FreeThrowCountSelectedStyle" TargetType="Button" BasedOn="{StaticResource FreeThrowCountButtonStyle}">
            <Setter Property="Background" Value="{StaticResource SelectedMadeColor}" />
        </Style>

        <!-- FREE THROW RESULT BUTTON -->
        <Style x:Key="FreeThrowResultButtonStyle" TargetType="Button" BasedOn="{StaticResource ActionSelectableButtonStyle}">
            <Setter Property="Background"  Value="Gray" />
            <Setter Property="Width" Value="120" />
            <Setter Property="Height" Value="40" />
        </Style>

        <Style x:Key="ShooterCourtPlayerStyle" TargetType="Button" BasedOn="{StaticResource CourtPlayerSelectableStyle}" po:Freeze="False">
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsSelectedAsFreeThrowShooter}" Value="True">
                    <Setter Property="BorderBrush" Value="White"/>
                    <Setter Property="BorderThickness" Value="4"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="ShooterBenchPlayerStyle" TargetType="Button" BasedOn="{StaticResource BenchPlayerSelectableStyle}" po:Freeze="False">
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsSelectedAsFreeThrowShooter}" Value="True">
                    <Setter Property="BorderBrush" Value="White"/>
                    <Setter Property="BorderThickness" Value="4"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="AssistCourtPlayerStyle" TargetType="Button" BasedOn="{StaticResource CourtPlayerSelectableStyle}" po:Freeze="False">
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsSelectedAsAssist}" Value="True">
                    <Setter Property="BorderBrush" Value="White"/>
                    <Setter Property="BorderThickness" Value="4"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="AssistBenchPlayerStyle" TargetType="Button" BasedOn="{StaticResource BenchPlayerSelectableStyle}" po:Freeze="False">
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsSelectedAsAssist}" Value="True">
                    <Setter Property="BorderBrush" Value="White"/>
                    <Setter Property="BorderThickness" Value="4"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <converters:IsTeamAToCourtStyleConverter x:Key="CourtPlayerStyleConverter"
                                     TeamAStyle="{StaticResource CourtAPlayerButtonStyle}"
                                     TeamBStyle="{StaticResource CourtBPlayerButtonStyle}" po:Freeze="False" />

        <converters:IsTeamAToBenchStyleConverter x:Key="BenchPlayerStyleConverter"
                                     TeamAStyle="{StaticResource BenchAPlayerButtonStyle}"
                                     TeamBStyle="{StaticResource BenchBPlayerButtonStyle}" po:Freeze="False" />
    </Window.Resources>

    <Window.InputBindings>
        <!-- Action: MADE (M) -->
        <KeyBinding Key="Q" Modifiers="Ctrl"
                Command="{Binding SelectActionCommand}"
                CommandParameter="MADE" />

        <!-- Action: MISSED (X) -->
        <KeyBinding Key="W" Modifiers="Ctrl"
                Command="{Binding SelectActionCommand}"
                CommandParameter="MISSED" />

        <!-- Action: FOUL (F) -->
        <KeyBinding Key="E" Modifiers="Ctrl"
                Command="{Binding SelectActionCommand}"
                CommandParameter="FOUL" />

        <!-- Turnover Selection (R) -->
        <KeyBinding Key="R" Modifiers="Ctrl"
                Command="{Binding StartTurnoverCommand}" />
    </Window.InputBindings>

    <TabControl>
        <TabItem Header="TEAM INFO">

            <controls:TeamInfoView DataContext="{Binding TeamInfoVM}" />
        </TabItem>
        <TabItem Header="MAIN">
            <Grid x:Name="___No_Name_" Background="#F9F9F9">
                <!-- Layout: Header, Event Buttons, Main Content -->
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="340"/>
                </Grid.ColumnDefinitions>

                <!-- ========= Row 0: Team Cards & Clock ========= -->

                <controls:ScoreBoardView Grid.Row="0" Grid.Column="0" 
                                         DataContext="{Binding ScoreBoardVM}" />

                <Grid Grid.Row="1" Grid.Column="0" Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="600"/>
                        <RowDefinition Height="200"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <!-- Row 2: Court (28×15) -->
                    <Grid Grid.Column="1" Grid.Row="0">
                        <!-- Base Court Control -->
                        <controls:Court x:Name="CourtControl"
                    VerticalAlignment="Stretch"
                    HorizontalAlignment="Stretch" />

                        <!-- Quick Shot Panel -->
                        <Grid Visibility="{Binding QuickShotPanelVisibility}"
      Background="#ccFFFFFF"
      Panel.ZIndex="10">
                            <Grid HorizontalAlignment="Center" VerticalAlignment="Center" Height="380">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="180"/>
                                    <ColumnDefinition Width="100"/>
                                    <ColumnDefinition Width="100"/>
                                    <ColumnDefinition Width="180"/>
                                </Grid.ColumnDefinitions>

                                <!-- TEAM A BENCH -->
                                <Border Background="#ccd3d3d3" Grid.Column="0" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="10" Margin="2" BorderThickness="1" BorderBrush="Black">
                                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <ItemsControl ItemsSource="{Binding EligibleTeamABenchShotPlayers}">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <WrapPanel Orientation="Vertical" FlowDirection="RightToLeft" Height="360"/>
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Button Content="{Binding DisplayName}"
                            Style="{StaticResource BenchAPlayerButtonStyle}"
                            Command="{Binding SelectPlayerCommand}"
                            Margin="4" />
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>
                                </Border>

                                <!-- TEAM A COURT -->
                                <Border Background="#cceeeeee" Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="10" Margin="2" BorderThickness="1" BorderBrush="Black">
                                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <ItemsControl ItemsSource="{Binding EligibleTeamACourtShotPlayers}">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <WrapPanel Orientation="Vertical"/>
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Button Content="{Binding DisplayName}"
                            Style="{StaticResource CourtAPlayerButtonStyle}"
                            Command="{Binding SelectPlayerCommand}"
                            Margin="4" />
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>
                                </Border>

                                <!-- TEAM B COURT -->
                                <Border Background="#cceeeeee" Grid.Column="2" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="10" Margin="2" BorderThickness="1" BorderBrush="Black">
                                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <ItemsControl ItemsSource="{Binding EligibleTeamBCourtShotPlayers}">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <WrapPanel Orientation="Vertical"/>
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Button Content="{Binding DisplayName}"
                            Style="{StaticResource CourtBPlayerButtonStyle}"
                            Command="{Binding SelectPlayerCommand}"
                            Margin="4" />
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>
                                </Border>

                                <!-- TEAM B BENCH -->
                                <Border Background="#ccd3d3d3" Grid.Column="3" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="10" Margin="2" BorderThickness="1" BorderBrush="Black">
                                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <ItemsControl ItemsSource="{Binding EligibleTeamBBenchShotPlayers}">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <WrapPanel Orientation="Vertical" Height="360"/>
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Button Content="{Binding DisplayName}"
                            Style="{StaticResource BenchBPlayerButtonStyle}"
                            Command="{Binding SelectPlayerCommand}"
                            Margin="4" />
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </Grid>

                        <!-- Assist Panel -->
                        <Grid Visibility="{Binding NoAssistButtonVisibility}"
      Panel.ZIndex="10" Background="#ccFFFFFF">
                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="400">

                                <!-- COURT PLAYERS -->
                                <Border Padding="10" Background="#cceeeeee" BorderThickness="1" BorderBrush="Black" Width="380">
                                    <ItemsControl ItemsSource="{Binding EligibleCourtAssistPlayers}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <WrapPanel HorizontalAlignment="Center" />
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Button Content="{Binding DisplayName}"
                            Style="{Binding IsTeamA, Converter={StaticResource CourtPlayerStyleConverter}}"
                            Command="{Binding SelectPlayerCommand}"
                            Margin="4" />
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </Border>

                                <!-- NO ASSIST -->
                                <Border>
                                    <Button Content="NO ASSIST"
                Command="{Binding NoAssistCommand}"
                HorizontalAlignment="Center"
                Height="60"
                Width="380"
                FontSize="24"
                FontWeight="Bold"
                Background="White"
                Foreground="Black"
                BorderBrush="Black"
                BorderThickness="1"
                Margin="10" />
                                </Border>
                                <!-- BENCH PLAYERS -->
                                <Border Background="#ccd3d3d3" Padding="10" BorderThickness="1" BorderBrush="Black" Width="380">
                                    <ItemsControl ItemsSource="{Binding EligibleBenchAssistPlayers}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <WrapPanel HorizontalAlignment="Center" Width="360"/>
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Button Content="{Binding DisplayName}"
                            Style="{Binding IsTeamA, Converter={StaticResource BenchPlayerStyleConverter}}"
                            Command="{Binding SelectPlayerCommand}"
                            Margin="4" />
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </Border>
                            </StackPanel>
                        </Grid>
                        <!-- Rebound Panel -->
                        <Grid Visibility="{Binding ReboundPanelVisibility}"
      Background="#ccFFFFFF"
      Panel.ZIndex="10" Height="500" Width="900">
                            <Grid HorizontalAlignment="Center" VerticalAlignment="Center" Height="500">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="160"/>
                                    <!-- Team A Bench -->
                                    <ColumnDefinition Width="100"/>
                                    <!-- Team A Court -->
                                    <ColumnDefinition Width="100"/>
                                    <!-- Team B Court -->
                                    <ColumnDefinition Width="160"/>
                                    <!-- Team B Bench -->
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="60"/>
                                    <RowDefinition Height="380"/>
                                    <RowDefinition Height="60"/>
                                </Grid.RowDefinitions>

                                <!-- TEAM A BENCH -->
                                <Border Background="#ccd3d3d3" Grid.Column="0" Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="5" BorderThickness="1" BorderBrush="Black">
                                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <ItemsControl ItemsSource="{Binding EligibleTeamABenchReboundPlayers}">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <WrapPanel Orientation="Vertical" FlowDirection="RightToLeft" Height="360"/>
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Button Content="{Binding DisplayName}"
Style="{StaticResource BenchAPlayerButtonStyle}"
Command="{Binding SelectPlayerCommand}"
Margin="4" />
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>
                                </Border>

                                <!-- TEAM A COURT -->
                                <Border Background="#cceeeeee" Grid.Column="1" Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="5" BorderThickness="1" BorderBrush="Black">
                                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <ItemsControl ItemsSource="{Binding EligibleTeamACourtReboundPlayers}" Height="360">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <WrapPanel Orientation="Vertical"/>
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Button Content="{Binding DisplayName}"
Style="{StaticResource CourtAPlayerButtonStyle}"
Command="{Binding SelectPlayerCommand}"
Margin="4" />
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>
                                </Border>
                                <Border Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2">
                                    <Button Content="{Binding TeamAName}"
Command="{Binding ReboundTeamACommand}"
Style="{StaticResource TeamAReboundStyle}"/>
                                </Border>
                                <Border Grid.Row="0" Grid.Column="2" Grid.ColumnSpan="2">
                                    <Button Content="{Binding TeamBName}"
Command="{Binding ReboundTeamBCommand}"
Style="{StaticResource TeamBReboundStyle}"/>
                                </Border>
                                <!-- CENTER REBOUND BUTTONS -->
                                <Border Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" HorizontalAlignment="Right">
                                    <Button Content="Block" Command="{Binding BlockCommand}" Style="{StaticResource BlockStyle}" Width="100" Height="50"/>
                                </Border>
                                <Border Grid.Row="2" Grid.Column="2" Grid.ColumnSpan="2" HorizontalAlignment="Left">
                                    <Button Content="24" Command="{Binding ShotClockCommand}" Style="{StaticResource ShotClockStyle}" Width="100" Height="50"/>
                                </Border>
                                <!-- TEAM B COURT -->
                                <Border Background="#cceeeeee" Grid.Column="2" Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="5" BorderThickness="1" BorderBrush="Black">
                                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <ItemsControl ItemsSource="{Binding EligibleTeamBCourtReboundPlayers}">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <WrapPanel Orientation="Vertical"/>
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Button Content="{Binding DisplayName}"
Style="{StaticResource CourtBPlayerButtonStyle}"
Command="{Binding SelectPlayerCommand}"
Margin="4" />
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>
                                </Border>

                                <!-- TEAM B BENCH -->
                                <Border Background="#ccd3d3d3" Grid.Column="3" Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="5" BorderThickness="1" BorderBrush="Black">
                                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <ItemsControl ItemsSource="{Binding EligibleTeamBBenchReboundPlayers}">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <WrapPanel Orientation="Vertical" Height="360"/>
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Button Content="{Binding DisplayName}"
Style="{StaticResource BenchBPlayerButtonStyle}"
Command="{Binding SelectPlayerCommand}"
Margin="4" />
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </Grid>

                        <Grid Visibility="{Binding BlockerSelectionVisibility}"
      Background="#ccFFFFFF"
      Panel.ZIndex="10">

                            <StackPanel HorizontalAlignment="Center"
                VerticalAlignment="Center">
                                <!-- COURT PLAYERS -->
                                <Border Background="#cceeeeee" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="10" Margin="2" BorderThickness="1" BorderBrush="Black">
                                    <ItemsControl ItemsSource="{Binding EligibleBlockCourtPlayers}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <WrapPanel HorizontalAlignment="Center" Width="360"/>
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Button Content="{Binding DisplayName}"
                            Style="{Binding IsTeamA, Converter={StaticResource CourtPlayerStyleConverter}}"
                            Command="{Binding SelectPlayerCommand}"
                            Margin="4"/>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </Border>
                                <!-- BENCH PLAYERS -->
                                <Border Background="#ccd3d3d3" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="10" Margin="2" BorderThickness="1" BorderBrush="Black">
                                    <ItemsControl ItemsSource="{Binding EligibleBlockBenchPlayers}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <WrapPanel HorizontalAlignment="Center" Width="360"/>
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Button Content="{Binding DisplayName}"
                            Style="{Binding IsTeamA, Converter={StaticResource BenchPlayerStyleConverter}}"
                            Command="{Binding SelectPlayerCommand}"
                            Margin="4"/>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </Border>
                            </StackPanel>
                        </Grid>

                        <Grid Visibility="{Binding TurnoverPanelVisibility}"
      Background="#ccFFFFFF"
      Panel.ZIndex="10" Height="500" Width="900">
                            <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="160"/>
                                    <ColumnDefinition Width="100"/>
                                    <ColumnDefinition Width="100"/>
                                    <ColumnDefinition Width="160"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="60" />
                                    <RowDefinition Height="380" />
                                    <RowDefinition Height="60" />
                                </Grid.RowDefinitions>

                                <!-- TEAM A BENCH -->
                                <Border Background="#ccd3d3d3" Grid.Column="0" Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="5" BorderThickness="1" BorderBrush="Black">
                                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <ItemsControl ItemsSource="{Binding EligibleTeamABenchTurnoverPlayers}">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <WrapPanel Orientation="Vertical" FlowDirection="RightToLeft" Height="360"/>
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Button Content="{Binding DisplayName}"
                            Style="{StaticResource BenchAPlayerButtonStyle}"
                            Command="{Binding SelectPlayerCommand}"
                            Margin="4" />
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>
                                </Border>

                                <!-- TEAM A COURT -->
                                <Border Background="#cceeeeee" Grid.Column="1" Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="5" BorderThickness="1" BorderBrush="Black">
                                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <ItemsControl ItemsSource="{Binding EligibleTeamACourtTurnoverPlayers}">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <WrapPanel Orientation="Vertical" Height="360"/>
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Button Content="{Binding DisplayName}"
                            Style="{StaticResource CourtAPlayerButtonStyle}"
                            Command="{Binding SelectPlayerCommand}"
                            Margin="4" />
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>
                                </Border>

                                <Border Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2">
                                    <Button Content="{Binding TeamAName}" Command="{Binding TurnoverTeamACommand}" Style="{StaticResource TeamATurnoverStyle}" />
                                </Border>
                                <Border Grid.Row="0" Grid.Column="2" Grid.ColumnSpan="2">
                                    <Button Content="{Binding TeamBName}" Command="{Binding TurnoverTeamBCommand}" Style="{StaticResource TeamBTurnoverStyle}" />
                                </Border>

                                <!-- TEAM B COURT -->
                                <Border Background="#cceeeeee" Grid.Column="2" Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="5" BorderThickness="1" BorderBrush="Black">
                                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <ItemsControl ItemsSource="{Binding EligibleTeamBCourtTurnoverPlayers}">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <WrapPanel Orientation="Vertical" Height="360"/>
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Button Content="{Binding DisplayName}"
                            Style="{StaticResource CourtBPlayerButtonStyle}"
                            Command="{Binding SelectPlayerCommand}"
                            Margin="4" />
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>
                                </Border>

                                <!-- TEAM B BENCH -->
                                <Border Background="#ccd3d3d3" Grid.Column="3" Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="5" BorderThickness="1" BorderBrush="Black">
                                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <ItemsControl ItemsSource="{Binding EligibleTeamBBenchTurnoverPlayers}">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <WrapPanel Orientation="Vertical" Height="360"/>
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Button Content="{Binding DisplayName}"
                            Style="{StaticResource BenchBPlayerButtonStyle}"
                            Command="{Binding SelectPlayerCommand}"
                            Margin="4" />
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </Grid>
                        <Grid Visibility="{Binding StealPanelVisibility}"
      Background="#33FFFFFF"
      Panel.ZIndex="10" Width="400">
                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                <!-- COURT PLAYERS -->
                                <Border Padding="10" Background="#cceeeeee" BorderThickness="1" BorderBrush="Black" Width="380">
                                    <ItemsControl ItemsSource="{Binding EligibleStealCourtPlayers}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <WrapPanel HorizontalAlignment="Center" Width="360"/>
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Button Content="{Binding DisplayName}"
                            Style="{Binding IsTeamA, Converter={StaticResource CourtPlayerStyleConverter}}"
                            Command="{Binding SelectPlayerCommand}"
                            Margin="4"/>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </Border>

                                <!-- NO STEAL -->
                                <Border>
                                    <Button Content="NO STEAL"
                Command="{Binding NoStealCommand}"
                HorizontalAlignment="Center"
                Height="60"
          Width="380"
          FontSize="24"
          FontWeight="Bold"
          Background="White"
          Foreground="Black"
          BorderBrush="Black"
          BorderThickness="1"
          Margin="10" />
                                </Border>

                                <!-- BENCH PLAYERS -->
                                <Border Background="#ccd3d3d3" Padding="10" BorderThickness="1" BorderBrush="Black" Width="380">
                                    <ItemsControl ItemsSource="{Binding EligibleStealBenchPlayers}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <WrapPanel HorizontalAlignment="Center" Width="360"/>
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Button Content="{Binding DisplayName}"
                            Style="{Binding IsTeamA, Converter={StaticResource BenchPlayerStyleConverter}}"
                            Command="{Binding SelectPlayerCommand}"
                            Margin="4"/>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </Border>
                            </StackPanel>
                        </Grid>
                        <Grid Visibility="{Binding FoulCommiterPanelVisibility}" Background="#ccFFFFFF" Panel.ZIndex="10" Height="500" Width="900">
                            <Grid HorizontalAlignment="Center" VerticalAlignment="Center" Height="500">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="160"/>
                                    <ColumnDefinition Width="100"/>
                                    <ColumnDefinition Width="100"/>
                                    <ColumnDefinition Width="160"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="60"/>
                                    <RowDefinition Height="380"/>
                                    <RowDefinition Height="60"/>
                                </Grid.RowDefinitions>

                                <Border Background="#ccd3d3d3" Grid.Column="0" Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="5" BorderThickness="1" BorderBrush="Black">
                                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <ItemsControl ItemsSource="{Binding EligibleTeamABenchFoulCommitterPlayers}">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <WrapPanel Orientation="Vertical" FlowDirection="RightToLeft" Height="360"/>
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Button Content="{Binding DisplayName}"
                                                    Style="{StaticResource BenchAPlayerButtonStyle}"
                                                    Command="{Binding SelectPlayerCommand}"
                                                    Margin="4" />
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>
                                </Border>

                                <Border Background="#cceeeeee" Grid.Column="1" Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="5" BorderThickness="1" BorderBrush="Black">
                                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <ItemsControl ItemsSource="{Binding EligibleTeamACourtFoulCommitterPlayers}">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <WrapPanel Orientation="Vertical"  Height="360"/>
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Button Content="{Binding DisplayName}"
                                                    Style="{StaticResource CourtAPlayerButtonStyle}"
                                                    Command="{Binding SelectPlayerCommand}"
                                                    Margin="4" />
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>
                                </Border>
                                <Border Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Background="#ccd3d3d3" BorderThickness="1" BorderBrush="Black" Margin="2,0,8,0">
                                    <Button Content="Coach" Command="{Binding CoachTechnicalTeamACommand}" Style="{StaticResource CoachTechnicalTeamAStyle}" />
                                </Border>
                                <Border Grid.Row="0" Grid.Column="2" Grid.ColumnSpan="2" Background="#ccd3d3d3" BorderThickness="1" BorderBrush="Black" Margin="8,0,2,0">
                                    <Button Content="Coach" Command="{Binding CoachTechnicalTeamBCommand}" Style="{StaticResource CoachTechnicalTeamBStyle}" />
                                </Border>
                                <Border Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Background="#ccd3d3d3" BorderThickness="1" BorderBrush="Black" Margin="2,0,8,0">
                                    <Button Content="Bench" Command="{Binding BenchTechnicalTeamACommand}" Style="{StaticResource BenchTechnicalTeamAStyle}" />
                                </Border>
                                <Border Grid.Row="2" Grid.Column="2" Grid.ColumnSpan="2" Background="#ccd3d3d3" BorderThickness="1" BorderBrush="Black" Margin="8,0,2,0">
                                    <Button Content="Bench" Command="{Binding BenchTechnicalTeamBCommand}" Style="{StaticResource BenchTechnicalTeamBStyle}" />
                                </Border>


                                <Border Background="#cceeeeee" Grid.Column="2" Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="5" BorderThickness="1" BorderBrush="Black">
                                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <ItemsControl ItemsSource="{Binding EligibleTeamBCourtFoulCommitterPlayers}">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <WrapPanel Orientation="Vertical"  Height="360"/>
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Button Content="{Binding DisplayName}"
                                                    Style="{StaticResource CourtBPlayerButtonStyle}"
                                                    Command="{Binding SelectPlayerCommand}"
                                                    Margin="4" />
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>
                                </Border>

                                <Border Background="#ccd3d3d3" Grid.Column="3" Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="5" BorderThickness="1" BorderBrush="Black">
                                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <ItemsControl ItemsSource="{Binding EligibleTeamBBenchFoulCommitterPlayers}">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <WrapPanel Orientation="Vertical"  Height="360"/>
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Button Content="{Binding DisplayName}"
                                                    Style="{StaticResource BenchBPlayerButtonStyle}"
                                                    Command="{Binding SelectPlayerCommand}"
                                                    Margin="4" />
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </Grid>
                        <Grid Visibility="{Binding FoulTypePanelVisibility}" Background="#ccFFFFFF" Panel.ZIndex="10" Width="900" Height="500">
                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                <TextBlock Text="Foul Type" FontSize="32" FontWeight="Bold" HorizontalAlignment="Center" Margin="10"/>
                                <WrapPanel HorizontalAlignment="Center" Width="900">
                                    <Button Style="{StaticResource FoulTypeButtonStyle}" Content="PERSONAL" Command="{Binding SelectFoulTypeCommand}" CommandParameter="personal"/>
                                    <Button Style="{StaticResource FoulTypeButtonStyle}" Content="SHOOTING" Command="{Binding SelectFoulTypeCommand}" CommandParameter="shooting"/>
                                    <Button Style="{StaticResource FoulTypeButtonStyle}" Content="OFFENSIVE" Command="{Binding SelectFoulTypeCommand}" CommandParameter="offensive"/>
                                    <Button Style="{StaticResource FoulTypeButtonStyle}" Content="TECHNICAL" Command="{Binding SelectFoulTypeCommand}" CommandParameter="technical"/>
                                    <Button Style="{StaticResource FoulTypeButtonStyle}" Content="DOUBLE PERSONAL" Command="{Binding SelectFoulTypeCommand}" CommandParameter="double personal"/>
                                    <Button Style="{StaticResource FoulTypeButtonStyle}" Content="UNSPORTSMANLIKE" Command="{Binding SelectFoulTypeCommand}" CommandParameter="unsportsmanlike"/>
                                    <Button Style="{StaticResource FoulTypeButtonStyle}" Content="DISQUALIFYING" Command="{Binding SelectFoulTypeCommand}" CommandParameter="disqualifying"/>
                                </WrapPanel>
                            </StackPanel>
                        </Grid>
                        <Grid Visibility="{Binding FreeThrowsAwardedPanelVisibility}" Background="#ccFFFFFF" Panel.ZIndex="10">
                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                <Border Background="#cceeeeee" Padding="10" Margin="2">
                                    <StackPanel>
                                        <TextBlock Text="Free Throws Awarded" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center" Margin="0 0 0 10"/>
                                        <WrapPanel HorizontalAlignment="Center" Margin="0 0 0 10">
                                            <Button Content="0" Style="{Binding FreeThrowCount0Style}" Command="{Binding SelectFreeThrowCountCommand}" CommandParameter="0" Margin="5" Padding="0"/>
                                            <Button Content="1" Style="{Binding FreeThrowCount1Style}" Command="{Binding SelectFreeThrowCountCommand}" CommandParameter="1" Margin="5" Padding="0"/>
                                            <Button Content="2" Style="{Binding FreeThrowCount2Style}" Command="{Binding SelectFreeThrowCountCommand}" CommandParameter="2" Margin="5" Padding="0"/>
                                            <Button Content="3" Style="{Binding FreeThrowCount3Style}" Command="{Binding SelectFreeThrowCountCommand}" CommandParameter="3" Margin="5" Padding="0"/>
                                        </WrapPanel>
                                    </StackPanel>
                                </Border>
                                <Border Background="#ccd3d3d3" Padding="10" Margin="2" Visibility="{Binding FreeThrowShooterVisibility}">
                                    <StackPanel>
                                        <TextBlock Text="Shooter" FontWeight="Bold" Margin="0 0 0 6" HorizontalAlignment="Center"/>
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>
                                            <Border Background="#cceeeeee" Grid.Row="0" Padding="10" Margin="2" BorderThickness="1" BorderBrush="Black" Width="380">
                                                <ItemsControl ItemsSource="{Binding EligibleFreeThrowCourtPlayers}">
                                                    <ItemsControl.ItemsPanel>
                                                        <ItemsPanelTemplate>
                                                            <WrapPanel Orientation="Horizontal" Width="360"/>
                                                        </ItemsPanelTemplate>
                                                    </ItemsControl.ItemsPanel>
                                                    <ItemsControl.ItemTemplate>
                                                        <DataTemplate>
                                                            <Button Content="{Binding DisplayName}"
                                                    Style="{StaticResource ShooterCourtPlayerStyle}"
                                                    Command="{Binding DataContext.SelectFreeThrowShooterCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                    CommandParameter="{Binding Player}"
                                                    Margin="4" />
                                                        </DataTemplate>
                                                    </ItemsControl.ItemTemplate>
                                                </ItemsControl>
                                            </Border>
                                            <Border Background="#ccd3d3d3" Grid.Row="1" Padding="10" Margin="2" BorderThickness="1" BorderBrush="Black" Width="380">
                                                <ItemsControl ItemsSource="{Binding EligibleFreeThrowBenchPlayers}">
                                                    <ItemsControl.ItemsPanel>
                                                        <ItemsPanelTemplate>
                                                            <WrapPanel Orientation="Horizontal" Width="360"/>
                                                        </ItemsPanelTemplate>
                                                    </ItemsControl.ItemsPanel>
                                                    <ItemsControl.ItemTemplate>
                                                        <DataTemplate>
                                                            <Button Content="{Binding DisplayName}"
                                                    Style="{StaticResource ShooterBenchPlayerStyle}"
                                                    Command="{Binding DataContext.SelectFreeThrowShooterCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                    CommandParameter="{Binding Player}"
                                                    Margin="4" />
                                                        </DataTemplate>
                                                    </ItemsControl.ItemTemplate>
                                                </ItemsControl>
                                            </Border>
                                        </Grid>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                                <Button Content="Confirm" Command="{Binding ConfirmFreeThrowsAwardedCommand}" Margin="10" HorizontalAlignment="Center" Style="{StaticResource FoulTypeButtonStyle}" />
                            </StackPanel>
                        </Grid>
                        <Grid Visibility="{Binding IsFouledPlayerSelectionActive, Converter={StaticResource BooleanToVisibilityConverter}}"
      Background="#33FFFFFF" Panel.ZIndex="10">
                            <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <Border Background="#cceeeeee" Grid.Row="0" Padding="10" Margin="2" BorderThickness="1" BorderBrush="Black" Width="380">
                                    <ItemsControl ItemsSource="{Binding EligibleFoulOnCourtPlayers}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <WrapPanel Orientation="Horizontal" Width="360" />
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Button Content="{Binding DisplayName}" Style="{Binding IsTeamA, Converter={StaticResource CourtPlayerStyleConverter}}" Command="{Binding SelectPlayerCommand}" Margin="4"/>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </Border>

                                <Border Background="#ccd3d3d3" Grid.Row="1" Padding="10" Margin="2" BorderThickness="1" BorderBrush="Black" Width="380">
                                    <ItemsControl ItemsSource="{Binding EligibleFoulOnBenchPlayers}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <WrapPanel Orientation="Horizontal" Width="360"/>
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Button Content="{Binding DisplayName}" Style="{Binding IsTeamA, Converter={StaticResource BenchPlayerStyleConverter}}" Command="{Binding SelectPlayerCommand}" Margin="4"/>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </Border>
                            </Grid>
                        </Grid>
                        <Grid Visibility="{Binding FreeThrowsPanelVisibility}" Background="#ccFFFFFF" Panel.ZIndex="10">
                            <StackPanel Orientation="Horizontal">
                                
                            </StackPanel>
                            <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition/>
                                    <ColumnDefinition/>
                                    <ColumnDefinition/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="Free Throws" FontSize="22" FontWeight="Bold" HorizontalAlignment="Center"/>
                                    <ItemsControl ItemsSource="{Binding FreeThrowResultRows}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <StackPanel Orientation="Vertical" Margin="10">
                                                    <TextBlock Text="{Binding Label}" FontSize="16" FontWeight="Bold" Margin="6 0 0 0"/>
                                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                                        <Button Content="MADE"
                                                        Style="{StaticResource FreeThrowResultButtonStyle}"
                                                        Command="{Binding SelectResultCommand}"
                                                        CommandParameter="MADE"
                                                        Background="{Binding MadeButtonBackground}"
                                                        Margin="4"/>
                                                        <Button Content="MISSED"
                                                        Style="{StaticResource FreeThrowResultButtonStyle}"
                                                        Command="{Binding SelectResultCommand}"
                                                        CommandParameter="MISSED"
                                                        Background="{Binding MissedButtonBackground}"
                                                        Margin="4"/>
                                                    </StackPanel>
                                                </StackPanel>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="Shooter" FontSize="22" FontWeight="Bold" Margin="0 0 0 6" HorizontalAlignment="Center"/>
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>
                                        <Border Background="#cceeeeee" HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Row="0" Padding="4" Margin="2" BorderThickness="1" BorderBrush="Black" Width="360">
                                            <ItemsControl ItemsSource="{Binding EligibleFreeThrowCourtPlayers}">
                                                <ItemsControl.ItemsPanel>
                                                    <ItemsPanelTemplate>
                                                        <WrapPanel Orientation="Horizontal" Width="340"/>
                                                    </ItemsPanelTemplate>
                                                </ItemsControl.ItemsPanel>
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <Button Content="{Binding DisplayName}"
                                                        Style="{StaticResource ShooterCourtPlayerStyle}"
                                                        Command="{Binding DataContext.SelectFreeThrowShooterCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                        CommandParameter="{Binding Player}"
                                                        Margin="2"/>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </Border>
                                        <Border Background="#ccd3d3d3" HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Row="1" Padding="4" Margin="2" BorderThickness="1" BorderBrush="Black" Width="360">
                                            <ItemsControl ItemsSource="{Binding EligibleFreeThrowBenchPlayers}">
                                                <ItemsControl.ItemsPanel>
                                                    <ItemsPanelTemplate>
                                                        <WrapPanel Orientation="Horizontal" Width="340"/>
                                                    </ItemsPanelTemplate>
                                                </ItemsControl.ItemsPanel>
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <Button Content="{Binding DisplayName}"
                                                        Style="{StaticResource ShooterBenchPlayerStyle}"
                                                        Command="{Binding DataContext.SelectFreeThrowShooterCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                        CommandParameter="{Binding Player}"
                                                        Margin="2"/>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </Border>
                                    </Grid>
                                </StackPanel>
                                <StackPanel Grid.Column="2">
                                    <TextBlock Text="Assist" FontWeight="Bold" Margin="10 0 0 6" HorizontalAlignment="Center"/>
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>
                                        <Border Background="#cceeeeee" HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Row="0" Padding="6" Margin="2" BorderThickness="1" BorderBrush="Black" Width="360">
                                            <ItemsControl ItemsSource="{Binding EligibleCourtAssistPlayers}">
                                                <ItemsControl.ItemsPanel>
                                                    <ItemsPanelTemplate>
                                                        <WrapPanel Orientation="Horizontal" Width="340"/>
                                                    </ItemsPanelTemplate>
                                                </ItemsControl.ItemsPanel>
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <Button Content="{Binding DisplayName}"
                                                        Style="{StaticResource AssistCourtPlayerStyle}"
                                                        Command="{Binding DataContext.SelectFreeThrowAssistCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                        CommandParameter="{Binding Player}"
                                                        IsEnabled="{Binding IsSelectableForAssist}"
                                                        Margin="2"/>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </Border>
                                        <Border Grid.Row="1">
                                            <Button Content="NO ASSIST"
                                            Command="{Binding DataContext.NoAssistFreeThrowCommand, RelativeSource={RelativeSource AncestorType=Window}}" Height="60"
                                            Width="360" FontSize="24" FontWeight="Bold" Background="White" Foreground="Black" BorderBrush="Black" BorderThickness="1" Margin="10" />
                                        </Border>
                                        <Border Background="#ccd3d3d3" HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Row="2" Padding="6" Margin="2" BorderThickness="1" BorderBrush="Black" Width="360">
                                            <ItemsControl ItemsSource="{Binding EligibleBenchAssistPlayers}">
                                                <ItemsControl.ItemsPanel>
                                                    <ItemsPanelTemplate>
                                                        <WrapPanel Orientation="Horizontal" Width="340"/>
                                                    </ItemsPanelTemplate>
                                                </ItemsControl.ItemsPanel>
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <Button Content="{Binding DisplayName}"
                                                        Style="{StaticResource AssistBenchPlayerStyle}"
                                                        Command="{Binding DataContext.SelectFreeThrowAssistCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                        CommandParameter="{Binding Player}"
                                                        IsEnabled="{Binding IsSelectableForAssist}"
                                                        Margin="2"/>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </Border>
                                    </Grid>
                                </StackPanel>
                            </Grid>
                        </Grid>
                        <!-- Timeout Panel -->
                        <Grid Visibility="{Binding TimeOutPanelVisibility}" Background="#33FFFFFF" Panel.ZIndex="10">
                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Background="White">
                                <TextBlock Text="Who called for time out?" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center" Margin="0 0 0 10"/>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Button Content="Team A" Style="{StaticResource TeamATimeoutStyle}" Command="{Binding TimeoutTeamACommand}" Margin="5"/>
                                    <Button Content="Team B" Style="{StaticResource TeamBTimeoutStyle}" Command="{Binding TimeoutTeamBCommand}" Margin="5"/>
                                </StackPanel>
                            </StackPanel>
                        </Grid>
                        <!-- Starting Five Panel -->
                        <Grid Visibility="{Binding StartingFivePanelVisibility}" Background="#ccFFFFFF" Panel.ZIndex="10">
                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="Auto">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="160"/>
                                        <ColumnDefinition Width="100"/>
                                        <ColumnDefinition Width="80"/>
                                        <ColumnDefinition Width="100"/>
                                        <ColumnDefinition Width="160"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="30" />
                                        <RowDefinition Height="400" />
                                    </Grid.RowDefinitions>
                                    <TextBlock Grid.Row="0" Grid.Column="0" Text="BENCH" FontSize="16" FontWeight="Bold" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                    <TextBlock Grid.Row="0" Grid.Column="1" Text="S5" FontSize="16" FontWeight="Bold" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                    <TextBlock Grid.Row="0" Grid.Column="3" Text="S5" FontSize="16" FontWeight="Bold" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                    <TextBlock Grid.Row="0" Grid.Column="4" Text="BENCH" FontSize="16" FontWeight="Bold" VerticalAlignment="Center" HorizontalAlignment="Center"/>

                                    <Border Grid.Row="1" Grid.Column="0" Background="#ccd3d3d3" HorizontalAlignment="Stretch" VerticalAlignment="Center" Height="380" Margin="2">
                                        <ItemsControl ItemsSource="{Binding TeamAStartingBenchPlayers}" VerticalAlignment="Center" HorizontalAlignment="Center">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <WrapPanel Orientation="Vertical" FlowDirection="RightToLeft" Height="360"/>
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Button Content="{Binding DisplayName}"
                                                            Style="{StaticResource CourtAPlayerButtonStyle}"
                                                            Command="{Binding DataContext.ToggleStartingFiveCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                            CommandParameter="{Binding}"
                                                            Margin="4" />
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </Border>
                                    <Border Grid.Row="1" Grid.Column="1" HorizontalAlignment="Stretch" VerticalAlignment="Center" Height="380" Margin="2">
                                        <ItemsControl ItemsSource="{Binding TeamAStartingFivePlayers}" VerticalAlignment="Center" HorizontalAlignment="Center">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <WrapPanel Orientation="Vertical" FlowDirection="RightToLeft" Height="360"/>
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Button Content="{Binding DisplayName}"
                                                            Style="{StaticResource CourtAPlayerButtonStyle}"
                                                            Command="{Binding DataContext.ToggleStartingFiveCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                            CommandParameter="{Binding}"
                                                            Margin="4" />
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </Border>

                                    <WrapPanel Grid.Row="0" Grid.RowSpan="2" Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Center">
                                        <Button Content="OK" Width="60" Height="60" FontSize="16" FontWeight="Bold" Background="DarkGreen" Foreground="White" Command="{Binding ConfirmStartingFiveCommand}" IsEnabled="{Binding IsStartingFiveConfirmEnabled}"/>
                                    </WrapPanel>

                                    <Border Grid.Row="1" Grid.Column="3" HorizontalAlignment="Stretch" VerticalAlignment="Center" Height="380" Margin="2" Background="#cceeeeee">
                                        <ItemsControl ItemsSource="{Binding TeamBStartingFivePlayers}" VerticalAlignment="Center" HorizontalAlignment="Center">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <WrapPanel Orientation="Vertical" Height="360"/>
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Button Content="{Binding DisplayName}"
                                                            Style="{StaticResource CourtBPlayerButtonStyle}"
                                                            Command="{Binding DataContext.ToggleStartingFiveCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                            CommandParameter="{Binding}"
                                                            Margin="4" />
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </Border>

                                    <Border Grid.Row="1" Grid.Column="4" Background="#ccd3d3d3" HorizontalAlignment="Stretch" VerticalAlignment="Center" Height="380" Margin="2">
                                        <ItemsControl ItemsSource="{Binding TeamBStartingBenchPlayers}" VerticalAlignment="Center" HorizontalAlignment="Center">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <WrapPanel Orientation="Vertical" Height="360"/>
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Button Content="{Binding DisplayName}"
                                                            Style="{StaticResource CourtBPlayerButtonStyle}"
                                                            Command="{Binding DataContext.ToggleStartingFiveCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                            CommandParameter="{Binding}"
                                                            Margin="4" />
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </Border>
                                </Grid>
                            </StackPanel>
                        </Grid>
                        <!-- Substitution Panel -->
                        <Grid Visibility="{Binding SubstitutionPanelVisibility}" Background="#ccFFFFFF" Panel.ZIndex="10">
                            <StackPanel HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Width="Auto">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="160"/>
                                        <ColumnDefinition Width="80"/>
                                        <ColumnDefinition Width="80"/>
                                        <ColumnDefinition Width="100"/>
                                        <ColumnDefinition Width="80"/>
                                        <ColumnDefinition Width="100"/>
                                        <ColumnDefinition Width="80"/>
                                        <ColumnDefinition Width="80"/>
                                        <ColumnDefinition Width="160"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="30" />
                                        <RowDefinition Height="400" />
                                    </Grid.RowDefinitions>
                                    <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="16" FontWeight="Bold" Grid.Row="0" Grid.Column="1" Text="IN"/>
                                    <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="16" FontWeight="Bold" Grid.Row="0" Grid.Column="2" Text="OUT"/>
                                    <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="16" FontWeight="Bold" Grid.Row="0" Grid.Column="0" Text="BENCH"/>
                                    <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="16" FontWeight="Bold" Grid.Row="0" Grid.Column="3" Text="COURT"/>
                                    <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="16" FontWeight="Bold" Grid.Row="0" Grid.Column="5" Text="COURT"/>
                                    <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="16" FontWeight="Bold" Grid.Row="0" Grid.Column="6" Text="OUT"/>
                                    <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="16" FontWeight="Bold" Grid.Row="0" Grid.Column="7" Text="IN"/>
                                    <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="16" FontWeight="Bold" Grid.Row="0" Grid.Column="8" Text="BENCH"/>
                                    <!-- Team A Bench -->
                                    <Border Grid.Row="1" Grid.Column="0" Background="#ccd3d3d3" HorizontalAlignment="Stretch" VerticalAlignment="Center" Height="380" Margin="2">
                                        <ItemsControl ItemsSource="{Binding TeamABenchPlayers}" VerticalAlignment="Center" HorizontalAlignment="Center">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <WrapPanel Orientation="Vertical" FlowDirection="RightToLeft" Height="360"/>
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Button Content="{Binding DisplayName}"
                                Style="{StaticResource CourtAPlayerButtonStyle}"
                                Command="{Binding DataContext.ToggleSubInCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                CommandParameter="{Binding}"
                                Margin="4" />
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </Border>

                                    <!-- Team A Sub In -->
                                    <Border Grid.Row="1" Grid.Column="1" HorizontalAlignment="Stretch" VerticalAlignment="Center" Height="380" Margin="2">
                                        <ItemsControl ItemsSource="{Binding TeamASubIn}"  VerticalAlignment="Center" HorizontalAlignment="Center">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <WrapPanel Orientation="Vertical" FlowDirection="RightToLeft" Height="360"/>
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Button Content="{Binding DisplayName}"
                                Style="{StaticResource CourtAPlayerButtonStyle}"
                                Command="{Binding DataContext.ToggleSubInCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                CommandParameter="{Binding}"
                                Margin="4" />
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </Border>

                                    <!-- Team A Sub Out -->
                                    <Border Grid.Row="1" Grid.Column="2" HorizontalAlignment="Stretch" VerticalAlignment="Center" Height="380" Margin="2">
                                        <ItemsControl ItemsSource="{Binding TeamASubOut}" VerticalAlignment="Center" HorizontalAlignment="Center">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <WrapPanel Orientation="Vertical" FlowDirection="RightToLeft" Height="360"/>
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Button Content="{Binding DisplayName}"
                                Style="{StaticResource CourtAPlayerButtonStyle}"
                                Command="{Binding DataContext.ToggleSubOutCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                CommandParameter="{Binding}"
                                Margin="4" />
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </Border>
                                    <!-- Team A Court -->
                                    <Border Grid.Row="1" Grid.Column="3" Background="#cceeeeee" HorizontalAlignment="Stretch" VerticalAlignment="Center" Height="380" Margin="2">
                                        <ItemsControl ItemsSource="{Binding TeamACourtPlayers}"  VerticalAlignment="Center" HorizontalAlignment="Center">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <WrapPanel Orientation="Vertical" FlowDirection="RightToLeft" Height="360"/>
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Button Content="{Binding DisplayName}"
                                Style="{StaticResource CourtAPlayerButtonStyle}"
                                Command="{Binding DataContext.ToggleSubOutCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                CommandParameter="{Binding}"
                                Margin="4" />
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </Border>
                                    <WrapPanel Grid.Row="0" Grid.RowSpan="2" Grid.Column="4" VerticalAlignment="Center" HorizontalAlignment="Center">
                                        <!-- Confirm Button -->
                                        <Button Content="OK" Width="60" Height="60" FontSize="16" FontWeight="Bold" Background="DarkGreen" Foreground="White" Command="{Binding ConfirmSubstitutionCommand}" IsEnabled="{Binding IsSubstitutionConfirmEnabled}"/>
                                    </WrapPanel>

                                    <!-- Team B Court -->
                                    <Border Grid.Row="1" Grid.Column="5" Background="#cceeeeee" HorizontalAlignment="Stretch" VerticalAlignment="Center" Height="380" Margin="2">
                                        <ItemsControl ItemsSource="{Binding TeamBCourtPlayers}"  VerticalAlignment="Center" HorizontalAlignment="Center">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <WrapPanel Orientation="Vertical" FlowDirection="RightToLeft" Height="360"/>
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Button Content="{Binding DisplayName}"
                                Style="{StaticResource CourtBPlayerButtonStyle}"
                                Command="{Binding DataContext.ToggleSubOutCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                CommandParameter="{Binding}"
                                Margin="4" />
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </Border>
                                    <!-- Team B Sub Out -->
                                    <Border Grid.Row="1" Grid.Column="6"  HorizontalAlignment="Stretch" VerticalAlignment="Center" Height="380" Margin="2">
                                        <ItemsControl ItemsSource="{Binding TeamBSubOut}"  VerticalAlignment="Center" HorizontalAlignment="Center">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <WrapPanel Orientation="Vertical"  Height="360"/>
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Button Content="{Binding DisplayName}"
                                Style="{StaticResource CourtBPlayerButtonStyle}"
                                Command="{Binding DataContext.ToggleSubOutCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                CommandParameter="{Binding}"
                                Margin="4" />
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </Border>

                                    <!-- Team B Sub In -->
                                    <Border Grid.Row="1" Grid.Column="7"  HorizontalAlignment="Stretch" VerticalAlignment="Center" Height="380" Margin="2">
                                        <ItemsControl ItemsSource="{Binding TeamBSubIn}"  VerticalAlignment="Center" HorizontalAlignment="Center">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <WrapPanel Orientation="Vertical" Height="360"/>
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Button Content="{Binding DisplayName}"
                                Style="{StaticResource CourtBPlayerButtonStyle}"
                                Command="{Binding DataContext.ToggleSubInCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                CommandParameter="{Binding}"
                                Margin="4" />
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </Border>

                                    <!-- Team B Bench -->
                                    <Border Grid.Row="1" Grid.Column="8" Background="#ccd3d3d3" HorizontalAlignment="Stretch" VerticalAlignment="Center" Height="380" Margin="2">

                                        <ItemsControl ItemsSource="{Binding TeamBBenchPlayers}"  VerticalAlignment="Center" HorizontalAlignment="Center">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <WrapPanel Orientation="Vertical" FlowDirection="LeftToRight" Height="360"/>
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Button Content="{Binding DisplayName}"
                                Style="{StaticResource CourtBPlayerButtonStyle}"
                                Command="{Binding DataContext.ToggleSubInCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                CommandParameter="{Binding}"
                                Margin="4" />
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </Border>
                                </Grid>
                            </StackPanel>
                        </Grid>


                    </Grid>

                    <Border Grid.Column="0" Grid.Row="0" BorderThickness="2" BorderBrush="#888" VerticalAlignment="Stretch" HorizontalAlignment="Left" Padding="5">
                        <ItemsControl ItemsSource="{Binding TeamAPlayers}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel Orientation="Vertical" FlowDirection="RightToLeft" Height="340"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>

                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button Content="{Binding DisplayName}"
                                            Style="{Binding ButtonStyle}" Margin="2"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Border>
                    <Border Grid.Column="2" Grid.Row="0" BorderThickness="2" BorderBrush="#888" VerticalAlignment="Stretch" HorizontalAlignment="Right" Padding="5">
                        <ItemsControl ItemsSource="{Binding TeamBPlayers}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel Orientation="Vertical" Height="340"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button Content="{Binding DisplayName}"
                                            Style="{Binding ButtonStyle}" Margin="2" />

                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Border>
                    <Border Grid.Column="0" Grid.Row="1" Grid.ColumnSpan="3" BorderThickness="2" BorderBrush="#888" VerticalAlignment="Top" HorizontalAlignment="Stretch" Padding="5" Margin="0,10">
                        <WrapPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,5">
                        <Button Content="MADE" Style="{StaticResource Made}" Command="{Binding SelectActionCommand}" CommandParameter="MADE" IsEnabled="{Binding IsActionSelectionActive}"/>
                        <Button Content="MISSED" Style="{StaticResource Missed}" Command="{Binding SelectActionCommand}" CommandParameter="Missed" IsEnabled="{Binding IsActionSelectionActive}"/>
                        <Button Content="FOUL (E)" Style="{StaticResource Foul}" Command="{Binding SelectActionCommand}" CommandParameter="Foul"/>
                        <Button Content="TURNOVER (R)" Style="{StaticResource Turnover}" Command="{Binding StartTurnoverCommand}"/>
                        <Button Content="TIME OUT" Style="{StaticResource TimeOut}" Command="{Binding StartTimeoutCommand}"/>
                        <Button Content="JUMP BALL" Style="{StaticResource JumpBall}" IsEnabled="False"/>
                        <Button Content="S5" Style="{StaticResource Sub}" Command="{Binding StartStartingFiveCommand}"/>
                            <Button Content="SUBSTITUTIONS" Style="{StaticResource Sub}" Command="{Binding StartSubstitutionCommand}" IsEnabled="False"/>
                            <Button Content="FREE THROWS" Style="{StaticResource FreeThrows}" Command="{Binding StartFreeThrowsCommand}" IsEnabled="False"/>
                            <Button Content="ASSIST" Style="{StaticResource Assist}" Command="{Binding StartAssistCommand}" IsEnabled="False"/>
                            <Button Content="REBOUND" Style="{StaticResource Rebound}" Command="{Binding StartReboundCommand}" IsEnabled="False"/>
                            <Button Content="STEAL" Style="{StaticResource Steal}" Command="{Binding StartStealCommand}" IsEnabled="False"/>
                    </WrapPanel>
                    </Border>
                </Grid>
                <!-- Play‐by‐Play Log -->
                <Border Grid.Row="0" Grid.RowSpan="2" Grid.Column="1" Margin="5" Background="#F9F9F9" BorderBrush="LightGray" BorderThickness="2">
                    <controls:PlayByPlayLog DataContext="{Binding PlayLog}" />
                </Border>
            </Grid>
        </TabItem>
        <TabItem Header="STATS">
            <controls:StatsView DataContext="{Binding StatsVM}" />
        </TabItem>
    </TabControl>
</Window>
