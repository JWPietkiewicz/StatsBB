<UserControl x:Class="StatsBB.UserControls.ActionPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.modernwpf.com/2019"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="150" d:DesignWidth="1300">
    <UserControl.Resources>
        <!-- Action Button Style with FluentUI -->
        <Style x:Key="FluentActionButtonStyle" TargetType="Button" BasedOn="{StaticResource BaseActionButtonStyle}">
            <Setter Property="Margin" Value="8"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="ui:ControlHelper.CornerRadius" Value="6"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
    </UserControl.Resources>
    
    <Border Background="{StaticResource SystemSurfaceColor}" 
            BorderBrush="{StaticResource SystemBorderColor}" 
            BorderThickness="1" CornerRadius="8" Padding="16">
        <Grid HorizontalAlignment="Center" VerticalAlignment="Bottom">
        <Grid.RowDefinitions>
            <RowDefinition Height="90" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Style="{StaticResource FluentActionButtonStyle}" Background="{StaticResource SuccessColor}">
                    <StackPanel Orientation="Horizontal">
                        <ui:FontIcon Glyph="&#xE73E;" FontSize="16" Margin="0,0,8,0"/>
                        <TextBlock Text="MADE"/>
                    </StackPanel>
                </Button>
                <Button Style="{StaticResource FluentActionButtonStyle}" Background="{StaticResource ErrorColor}">
                    <StackPanel Orientation="Horizontal">
                        <ui:FontIcon Glyph="&#xE711;" FontSize="16" Margin="0,0,8,0"/>
                        <TextBlock Text="MISSED"/>
                    </StackPanel>
                </Button>
                <Button Style="{StaticResource FluentActionButtonStyle}" Background="{StaticResource WarningColor}">
                    <StackPanel Orientation="Horizontal">
                        <ui:FontIcon Glyph="&#xE7BA;" FontSize="16" Margin="0,0,8,0"/>
                        <TextBlock Text="FOUL"/>
                    </StackPanel>
                </Button>
                <Button Style="{StaticResource FluentActionButtonStyle}" Background="{StaticResource SystemControlBackgroundColor}">
                    <StackPanel Orientation="Horizontal">
                        <ui:FontIcon Glyph="&#xE7AD;" FontSize="16" Margin="0,0,8,0"/>
                        <TextBlock Text="TURNOVER"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
