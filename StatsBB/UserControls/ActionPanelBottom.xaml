<UserControl x:Class="StatsBB.UserControls.ActionPanelBottom"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="150" d:DesignWidth="1000">
    <UserControl.Resources>
        <!-- Use global button styles from MainWindow -->
    </UserControl.Resources>
    <Grid VerticalAlignment="Top" HorizontalAlignment="Center" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="90" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="100"/>
        </Grid.ColumnDefinitions>
        <Button Grid.Column="0" Style="{StaticResource BaseActionButtonStyle}" Content="JUMP BALL" />
        <Button Grid.Column="1" Style="{StaticResource BaseActionButtonStyle}" Content="SUB" />
        <Button Grid.Column="2" Style="{StaticResource BaseActionButtonStyle}" Content="TIME OUT" />
        
    </Grid>
</UserControl>
