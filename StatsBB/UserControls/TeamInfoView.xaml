<UserControl x:Class="StatsBB.UserControls.TeamInfoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:converters="clr-namespace:StatsBB.Converters"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <converters:ColorAvailabilityConverter x:Key="ColorAvailabilityConverter"/>
        <converters:ColorSelectedConverter x:Key="ColorSelectedConverter"/>
        <Style x:Key="TeamColorButtonStyle" TargetType="RadioButton">
            <Setter Property="Width" Value="60"/>
            <Setter Property="Height" Value="60"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="border"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center"
                                      VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="BorderThickness" Value="4"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="{Binding Foreground, RelativeSource={RelativeSource TemplatedParent}}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PlayerEditTextStyle" TargetType="TextBox">
            <EventSetter Event="PreviewKeyDown" Handler="PlayerTextBox_PreviewKeyDown" />
        </Style>

        <Style x:Key="PlayerDataGridStyle" TargetType="DataGrid">
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="AlternatingRowBackground" Value="#FFF0F0F0"/>
            <Setter Property="RowHeaderWidth" Value="0"/>
            <Setter Property="RowHeight" Value="30"/>
        </Style>
    </UserControl.Resources>
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0" Margin="0,0,10,0" Background="LightGray">
            <TextBlock Text="Home Team" FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBlock Text="Team Name"/>
            <TextBox Text="{Binding HomeTeamName, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock Text="Team Color" Margin="0,5,0,0"/>
            <ItemsControl ItemsSource="{Binding ColorOptions}" Margin="0,2">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel ItemWidth="60" ItemHeight="60" MaxWidth="520"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <RadioButton Style="{StaticResource TeamColorButtonStyle}" Margin="2"
                                 Background="{Binding ColorBrush}" Foreground="{Binding TextBrush}" GroupName="TeamAColorGroup">
                            <RadioButton.Content>99</RadioButton.Content>
                            <RadioButton.IsChecked>
                                <MultiBinding Converter="{StaticResource ColorSelectedConverter}" Mode="OneWay">
                                    <Binding Path="DataContext.TeamAColorOption" RelativeSource="{RelativeSource AncestorType=Window}" Mode="OneWay"/>
                                    <Binding Mode="OneWay" />
                                </MultiBinding>
                            </RadioButton.IsChecked>
                            <RadioButton.Command>
                                <Binding Path="DataContext.SelectTeamAColorCommand" RelativeSource="{RelativeSource AncestorType=Window}"/>
                            </RadioButton.Command>
                            <RadioButton.CommandParameter>
                                <Binding />
                            </RadioButton.CommandParameter>
                            <RadioButton.IsEnabled>
                                <MultiBinding Converter="{StaticResource ColorAvailabilityConverter}" Mode="OneWay">
                                    <Binding Path="DataContext.TeamBColorOption" RelativeSource="{RelativeSource AncestorType=Window}" Mode="OneWay"/>
                                    <Binding Mode="OneWay" />
                                </MultiBinding>
                            </RadioButton.IsEnabled>
                        </RadioButton>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            <DataGrid x:Name="HomeGrid" ItemsSource="{Binding HomePlayers}" AutoGenerateColumns="False" Margin="0,5,0,0" CanUserAddRows="False" SelectionUnit="Cell" Style="{StaticResource PlayerDataGridStyle}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="#" Binding="{Binding Number}" Width="40" EditingElementStyle="{StaticResource PlayerEditTextStyle}"/>
                    <DataGridTextColumn Header="First" Binding="{Binding FirstName}" Width="*" EditingElementStyle="{StaticResource PlayerEditTextStyle}"/>
                    <DataGridTextColumn Header="Last" Binding="{Binding LastName}" Width="*" EditingElementStyle="{StaticResource PlayerEditTextStyle}"/>
                    <DataGridTemplateColumn Header="Play" Width="50">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding IsPlaying, Mode=TwoWay}" HorizontalAlignment="Center"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
            <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                <Button Content="Load Team" Command="{Binding LoadHomeTeamCommand}" Margin="0,0,5,0"/>
                <Button Content="Save Team" Command="{Binding SaveHomeTeamCommand}"/>
            </StackPanel>
        </StackPanel>
        <StackPanel Grid.Column="1" Background="LightGray">
            <TextBlock Text="Away Team" FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBlock Text="Team Name"/>
            <TextBox Text="{Binding AwayTeamName, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock Text="Team Color" Margin="0,5,0,0"/>
            <ItemsControl ItemsSource="{Binding ColorOptions}" Margin="0,2">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel ItemWidth="60" ItemHeight="60" MaxWidth="520"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <RadioButton Style="{StaticResource TeamColorButtonStyle}" Margin="2"
                                 Background="{Binding ColorBrush}" Foreground="{Binding TextBrush}" GroupName="TeamBColorGroup">
                            <RadioButton.Content>99</RadioButton.Content>
                            <RadioButton.IsChecked>
                                <MultiBinding Converter="{StaticResource ColorSelectedConverter}" Mode="OneWay">
                                    <Binding Path="DataContext.TeamBColorOption" RelativeSource="{RelativeSource AncestorType=Window}" Mode="OneWay"/>
                                    <Binding Mode="OneWay" />
                                </MultiBinding>
                            </RadioButton.IsChecked>
                            <RadioButton.Command>
                                <Binding Path="DataContext.SelectTeamBColorCommand" RelativeSource="{RelativeSource AncestorType=Window}"/>
                            </RadioButton.Command>
                            <RadioButton.CommandParameter>
                                <Binding />
                            </RadioButton.CommandParameter>
                            <RadioButton.IsEnabled>
                                <MultiBinding Converter="{StaticResource ColorAvailabilityConverter}" Mode="OneWay">
                                    <Binding Path="DataContext.TeamAColorOption" RelativeSource="{RelativeSource AncestorType=Window}" Mode="OneWay"/>
                                    <Binding Mode="OneWay" />
                                </MultiBinding>
                            </RadioButton.IsEnabled>
                        </RadioButton>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            <DataGrid x:Name="AwayGrid" ItemsSource="{Binding AwayPlayers}" AutoGenerateColumns="False" Margin="0,5,0,0" CanUserAddRows="False" SelectionUnit="Cell" Style="{StaticResource PlayerDataGridStyle}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="#" Binding="{Binding Number}" Width="40" EditingElementStyle="{StaticResource PlayerEditTextStyle}"/>
                    <DataGridTextColumn Header="First" Binding="{Binding FirstName}" Width="*" EditingElementStyle="{StaticResource PlayerEditTextStyle}"/>
                    <DataGridTextColumn Header="Last" Binding="{Binding LastName}" Width="*" EditingElementStyle="{StaticResource PlayerEditTextStyle}"/>
                    <DataGridTemplateColumn Header="Play" Width="50">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding IsPlaying, Mode=TwoWay}" HorizontalAlignment="Center"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
            <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                <Button Content="Load Team" Command="{Binding LoadAwayTeamCommand}" Margin="0,0,5,0"/>
                <Button Content="Save Team" Command="{Binding SaveAwayTeamCommand}"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
